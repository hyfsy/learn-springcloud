server:
  port: 8354
spring:
  application:
    name: eureka-service-3-1
eureka:
  client:
    service-url:
      defaultZone: http://localhost8305:8305/eureka/
    instance:
      instance-id: ${spring.application.name}
services: >